# 验证记录

## 2026-09-06

核心逻辑自动测试：8 项通过。

覆盖带引号与多行 CSV 的往返、非法文件与重复 id、100 条限制、数字归一化与重复数字、占位符中的数字排除、术语词边界、仅确认修改影响导出、表格公式前缀保护。

应用编译、接口、发布仍待验证。尚未进行目标用户测试，未测量翻译准确率和审校时间改善。真实模型调用尚未配置或验证。

更新：TypeScript 类型检查通过，生产编译通过。首页本地 HTTP 200；未配置模型时接口返回明确的 503 提示。三个 WebMCP 工具已在浏览器注册并验证：有效 CSV 导入后可读回与导出；非法 CSV 被拒绝且原数据保持不变。未进行完整浏览器点击或视觉回归测试。

## 2026-09-07

完整源码已恢复到普通文件目录，并建立本地工程基线提交。核心逻辑、类型检查、静态检查和生产构建重新通过，本地首页返回 HTTP 200。

AI 审校边界已提取为可单元测试的输入验证、提示词构造和模型响应解析模块。新增 6 项测试，覆盖合法请求、空文案、重复 ID、空原文、非法术语、提示词版本与不可信数据声明、结果来源、未知 ID、非法类别，以及语境不足与建议内容冲突。当前自动测试共 14 项，全部通过。

AI 结果现要求包含问题类别、严重程度、中文理由、具体证据、建议译文和是否需要补充语境，并由服务端增加模型名称与提示词版本。真实模型调用仍未配置或验证，这些结果只能证明接口边界和失败保护，不能证明模型审校效果。
